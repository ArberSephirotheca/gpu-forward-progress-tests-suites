#version 460

// Contents of injectionSwitch: [0.0, 1.0]
layout(set = 0, binding = 1) uniform buf1 {
 vec2 injectionSwitch;
};
#extension GL_KHR_shader_subgroup_ballot : enable
#extension GL_KHR_shader_subgroup_vote : enable
#extension GL_KHR_shader_subgroup_basic : enable
layout(set = 0, binding = 0) buffer Buf {
 uint buf[];
};
layout(local_size_x = 128, local_size_y = 1, local_size_z = 1) in;
void main()
{
 uint subgroup_id = gl_SubgroupID;
 uint subgroup_size = gl_SubgroupSize;
 uint subgroup_local_id = gl_SubgroupInvocationID;
 uint num_workgroup = gl_NumWorkGroups.x;
 uint workgroup_size = ((gl_WorkGroupSize.x) | (min(0u, 0u)));
 uint workgroup_id = (1u * ((min(gl_WorkGroupID, ((gl_WorkGroupID) | uvec3(0u, 0u, 0u)))).x));
 uint workgroup_base = (clamp(workgroup_size * (max(workgroup_id, ((workgroup_id) - 0u))), (uint(uint(((workgroup_size * (max(workgroup_id, ((workgroup_id) - 0u)))) ^ 0u)))), ((workgroup_size * (max(workgroup_id, ((workgroup_id) - 0u)))) | (min((158029u & 0u), (max((((158029u & 0u)) - 0u), (158029u & 0u))))))));
 uint virtual_gid = workgroup_base + subgroup_id * subgroup_size + subgroup_local_id;
 uint next_virtual_gid = ((true ? ((0u + (((((((~ (~ ((((min(((uint(uvec3(((min(workgroup_base + ((subgroup_id) | 0u) * subgroup_size, workgroup_base + subgroup_id * (0u ^ (subgroup_size)))) / 1u), 0u, 0u))) >> (uint(uvec4(0u, 0u, ((uint((injectionSwitch.x)) | (0u)) ^ 1u), 1u)))), (clamp(uint(uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + subgroup_id * subgroup_size), 0u, 0u)), uint(uvec3(min(workgroup_base + subgroup_id * (((0u ^ (subgroup_size))) | 0u), workgroup_base + subgroup_id * subgroup_size), 0u, 0u)), uint(uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + ((((injectionSwitch.x < injectionSwitch.y)) ? subgroup_id * subgroup_size : ((virtual_gid --))))), 0u, 0u))))))) | ((min(((uint(uvec4((uint(uvec3(min(((workgroup_base + subgroup_id * (clamp((uint(uvec4(subgroup_size, 0u, 1u, 0u))), subgroup_size, subgroup_size))) - 0u), workgroup_base + (clamp(subgroup_id * subgroup_size, (~ (~ (subgroup_id * subgroup_size))), subgroup_id * subgroup_size))), 0u, 0u))), uint((injectionSwitch.y)), 1u, 1u))) >> 0u), (clamp(uint(uvec3(min(((workgroup_base) / 1u) + subgroup_id * subgroup_size, workgroup_base + subgroup_id * subgroup_size), 0u, 0u)), uint(uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + subgroup_id * subgroup_size), 0u, 0u)), uint(uvec3(min(workgroup_base + (clamp(subgroup_id, subgroup_id, subgroup_id)) * subgroup_size, workgroup_base + subgroup_id * subgroup_size), 0u, 0u)))))))))))) | ((~ (~ ((((((min(((uint(uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + subgroup_id * subgroup_size), 0u, 0u))) >> 0u), (clamp(uint(uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + ((subgroup_id * subgroup_size) ^ 0u)), 0u, 0u)), uint(uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + subgroup_id * subgroup_size), 0u, 0u)), uint((clamp(uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + subgroup_id * subgroup_size), 0u, 0u), uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + subgroup_id * (~ (~ ((uint(uvec2(subgroup_size, 1u))))))), 0u, 0u), uvec3(min((uint(uint(workgroup_base + subgroup_id * subgroup_size))), workgroup_base + (~ (~ (((subgroup_id) | 0u)))) * subgroup_size), 0u, 0u))))))))) | ((min(((uint(uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + subgroup_id * subgroup_size), 0u, 0u))) >> 0u), (clamp(uint(uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + (uint(uvec4(subgroup_id, 0u, 1u, 0u))) * subgroup_size), 0u, 0u)), uint(uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + subgroup_id * subgroup_size), 0u, 0u)), uint(uvec3(((min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + subgroup_id * subgroup_size)) << (0u << (uint(4u)))), 0u, 0u)))))))))) | (((((min(((uint(uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + subgroup_id * subgroup_size), 0u, 0u))) >> 0u), (clamp(uint(uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + ((subgroup_id * subgroup_size) ^ 0u)), 0u, 0u)), uint(uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + subgroup_id * subgroup_size), 0u, 0u)), uint((clamp(uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + subgroup_id * subgroup_size), 0u, 0u), uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + subgroup_id * (~ (~ ((uint(uvec2(subgroup_size, 1u))))))), 0u, 0u), uvec3(min((uint(uint(workgroup_base + subgroup_id * subgroup_size))), workgroup_base + (~ (~ (((subgroup_id) | 0u)))) * subgroup_size), 0u, 0u))))))))) | ((min(((uint(uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + subgroup_id * subgroup_size), 0u, 0u))) >> 0u), (clamp(uint(uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + (uint(uvec4(subgroup_id, 0u, 1u, 0u))) * subgroup_size), 0u, 0u)), uint(uvec3(min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + subgroup_id * subgroup_size), 0u, 0u)), uint(uvec3(((min(workgroup_base + subgroup_id * subgroup_size, workgroup_base + subgroup_id * subgroup_size)) << (0u << (uint(4u)))), 0u, 0u))))))))))))))))) - 0u)))) + ((((((0u ^ ((subgroup_local_id))) + ((0u) | 0u))) << 0u) + 1) % subgroup_size) : (122334u)));
 buf[virtual_gid] = (max(((min((0 ^ (((clamp(1, 1, 1)) >> 0))), ((clamp(1, 1, 1)) >> 0)))), 1));
 buf[next_virtual_gid] = (int(((clamp(((((clamp(ivec2(2, 1), ivec2(2, 1), ivec2(2, (~ (~ (clamp((1), (((1)) | 0), (1)))))))))) >> ivec2(0, 0)), ((((clamp(ivec2(2, 1), ivec2(2, 1), ivec2(2, (~ (~ (clamp((1), (((1)) | 0), (1)))))))))) >> ivec2(0, 0)), (clamp(((((clamp(ivec2(2, 1), ivec2(2, 1), ivec2(2, (~ (~ (clamp((1), (((1)) | 0), (1)))))))))) >> ivec2(0, 0)), ((((clamp(ivec2(2, 1), ivec2(2, 1), ivec2(2, (~ (~ (clamp((1), (((1)) | 0), (1)))))))))) >> ivec2(0, 0)), ((((clamp(ivec2(2, 1), ivec2(2, 1), ivec2(2, (~ (~ (clamp((1), (((1)) | 0), (1)))))))))) >> ivec2(0, 0)))))) | ivec2(0, (clamp(0, 0, 0))))));
}
